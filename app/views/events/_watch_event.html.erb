<%= form_for(current_user.event_volunteers.build, remote: true) do |f| %>
   
  <div>
    <%= hidden_field_tag :event_id, @event.id %>
    <%= hidden_field_tag :attending, "false" %>
  </div>

  <%= f.submit "Add to watchlist", class: "btn btn-primary il-button" %>

<% end %>