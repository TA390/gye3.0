<% provide(:title, 'Volunteer') %>
<p id="notice"><%= notice %></p>

<body class="v_index"> 
  <div class="dropdownbarbox">
    	<div class="container">
          <div class="dropdownbox_des">
 				    <h1> Volunteer Search Tool </h1> 
				    <h2> Find Eligible Volunteers! </h2>
         	</div>
      </div>
  </div>

  <p></p>
  
  <div class="form-inline">
    <%= form_for :volunteer, url: {action: "search"}, html:{method: 'put'} do |f| %>
    
      
       <div class="form-group">
         <%= f.text_field :name, placeholder: "Volunteer name", class: 'form-control' %>
       </div>
      
       <div class="form-group">
         <%= f.text_field :tags, :size => 30, placeholder: "Search by volunteer interest", class: 'form-control' %>
       </div>

       <div class="form-group">
         <%= f.text_field :location, placeholder: "Volunteer location", class: 'form-control' %>
       </div>

       <div class="form-group">
         <%= f.text_field :gender, placeholder: "Gender", class: 'form-control' %>
       </div>
       
     
       <div class="form-group">      
         <%= f.submit "Search", class: "btn btn-default" %>
       </div>  
     </div>
   <% end %>
  
    <p></p>
  
  <div class = "container">  
    <div class="volunteers col-md-12">
      <%= will_paginate %>
      <% @volunteers.each do |volunteer| %>
        <div class = "one_volunteer">
          <div class ="row">

            <div class="col-md-3">
              <div class="container">
                <div class ="volunteer_pic">
                  <img class="img-circle" src="http://wikivillage.co.za/sites/default/files/media/garden-route-spca/garden-route-spca1.jpg" alt="description here "/>
                  
                      
              
                   <!-- tag icons associated with volunteers -->
                  <div class="icon-holder">
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 1).exists? %>
                      <i class="fa fa-heartbeat"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 2).exists? %>
                     <i class="fa fa-paint-brush"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 3).exists? %>
                     <i class="fa fa-paw"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 4).exists? %>
                     <i class="fa fa-cutlery"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 5).exists? %>
                     <i class="fa fa-ambulance"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 6).exists? %>
                     <i class="fa fa-book"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 7).exists? %>
                      <i class="fa fa-smile-o"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 8).exists? %>
                     <i class="fa fa-home"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 9).exists? %>
                     <i class="fa fa-child"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 10).exists? %>
                      <i class="fa fa-plane"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 11).exists? %>
                     <i class="fa fa-wheelchair"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 12).exists? %>
                     <i class="fa fa-users"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 13).exists? %>
                      <i class="fa fa-suitcase"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 14).exists? %>
                     <i class="fa fa-life-ring"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 15).exists? %>
                     <i class="fa fa-leaf"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 16).exists? %>
                      <i class="fa fa-arrows"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 17).exists? %>
                     <i class="fa fa-ship"></i>
                   <% end %>
                   <% if volunteer.volunteer_tags.where("tag_id = ?", 18).exists? %>
                     <i class="fa fa-futbol-o"></i>
                   <% end %>
                  </div>  <!-- end of tag icons associated with volunteers -->
                  
                  
                </div>
              </div>
            </div>

            <div class="col-md-9 ">
              <h3><%=volunteer.name %> <%=volunteer.last_name %></h3>
                <h4>Bio:</h4>
                <p> [Bio goes here] </p>
                <p> Joining date: <%=volunteer.created_at%> </p>

              <%=button_to 'Show Profile', volunteer_path(volunteer), :method => :get, :class => "btn btn-primary"%>

            </div>
          </div>
          <!--End of Row -->
        </div>
        <!--End of one_volunteer wrappper -->
      <% end %>
      <%= will_paginate %>
    </div>
  </div>

  <h1>Listing Volunteers</h1>
  <table>
    <thead>
      <tr>
        <th>First Name</th>
        <th>Email</th>
        <th>Location</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @volunteers.each do |volunteer| %>
        <tr>

          <td><%= volunteer.name %></td>
          <td><%= volunteer.email %></td>
          <td><%= volunteer.location %></td>
          <td><%= link_to 'Show', volunteer_path(volunteer) %></td>
          <td><%= link_to 'Edit', edit_volunteer_path(volunteer) %></td>
          <td><%= link_to 'Destroy', volunteer_path(volunteer), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
</body>



<!--
    t.string   "name"
    t.string   "email"
    t.datetime "created_at",                        null: false
    t.datetime "updated_at",                        null: false
    t.string   "password_digest"
    t.string   "location"
    t.string   "remember_digest"
    t.string   "activation_digest"
    t.boolean  "activated",         default: false
    t.datetime "activated_at"
    t.string   "type"
    t.string   "last_name"
    t.string   "gender"
    t.string   "phone_number"
    t.string   "url"

 <div class="btn-group">
                  <button type="button" class="btn btn-info"> See More </button>
                </div>


-->

<br>

